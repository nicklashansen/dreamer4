JAX Reference Corrections — Deferred
=====================================
Source: https://github.com/edwhu/dreamer4-jax

1. MTP default L=2 (not 8)
   - JAX uses L=2 for both policy and reward heads
   - Our code defaults to mtp_length=8
   - Keep configurable, but change default when ready

2. Num bins = 101 (not 255), with different ranges per head
   - Reward head: 101 bins in symlog space [-3.0, 3.0]
   - Value head:  101 bins in symlog space [-8.0, 8.0]
   - Our code defaults to 255 bins, range [-20, 20]

4. Two-hot encoding uses searchsorted (not floor arithmetic)
   - JAX uses jnp.searchsorted for bin lookup
   - Our floor-based indexing is equivalent for uniform bins
   - Could switch to torch.searchsorted for robustness

Already applied:
3. SwiGLU MLPs for heads ✓
5. Phase 2 finetunes dynamics with shortcut/flow loss ✓ (in training script)
6. stop_gradient on h_t before policy in Phase 3 ✓ (in training script)

Other JAX reference details for training scripts:
- Phase 2 batch: B=64, T=64, lr=3e-4
- Phase 2 dynamics loss: MSE flow + bootstrap self-consistency (w = 0.9*tau + 0.1)
- Phase 2 bootstrap_start = 5000 steps, self_fraction = 0.25
- Phase 3: horizon=32, context_length=16, imagination_d=1/4
- Phase 3: only policy + value heads trainable; everything else frozen
- Phase 3: policy_fn receives h_t.detach() but policy params are differentiable
- Denoising uses EMA form: z = (1-alpha)*z + alpha*z_clean_pred
  where alpha = dt/(1-tau), equivalent to Euler step
- Context always at tau=1.0 (no corruption during denoising despite paper mention)
